
import { timeFormater } from 'corporate-dashboard/helpers/time-formater';
import { module, test } from 'qunit';

module('Unit | Helper | time formater');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = timeFormater([42]);
  assert.ok(result);
});

