
import { storeToArray } from 'corporate-dashboard/helpers/store-to-array';
import { module, test } from 'qunit';

module('Unit | Helper | store to array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = storeToArray([42]);
  assert.ok(result);
});

